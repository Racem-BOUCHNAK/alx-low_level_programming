#include "main.h"
/**
 *_strncpy - entry point of the code
 *@dest:var
 *@src:var
 *@n:var
 * Return: Always 0 (Success)
 */
char *_strncpy(char *dest, char *src, int n)
{
for (i = 0; i < n && src[i] != '\0'; i++)
dest[i] = src[i];
for ( ; i < n; i++)
dest[i] = '\0';
return (dest);
}
