#include "main.h"
/**
*main - entry point of the code
*the required work
* Return: Always 0 (Success)
*/
char *_memcpy(char *dest, char *src, unsigned int n)
{
int i = 0;
for (; n > 0; n--,i++)
dest[i] = src[i];
return (dest);
}
