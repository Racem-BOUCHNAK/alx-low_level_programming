#include <stdio.h>
#include "main.h"
/**
 *print_array - entry point of the code
 *@a:pointer
 *@n:var
 * Return: Always 0 (Success)
 */
void print_array(int *a, int n)
{
int i = 0;
for (; i < n; i++)
{
printf("%d", a[i]);
if (i < n - 1)
printf(", ");
}
printf('\n');
}
