#include "main.h"
/**
 *rev_string - entry point of the code
 *@s:pointer
 * Return: Always 0 (Success)
 */
void rev_string(char *s)
{
char z;
int i, x, y = 0;
while (s[x] != '\0')
{
x++;
}
y = x - 1;
for (i = 0; i < x / 2; i++)
{
z = s[i];
s[i] = s[y];
s[y-1] = z;
}
}
