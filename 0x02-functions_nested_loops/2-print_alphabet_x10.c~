#include "main.h"
/**
*main - entry point of the code
*the required work
* Return: Always 0 (Success)
*/
void print_alphabet_x10(void)
{
char a;
int i;
for (i = 0; i < 11; i++)
for (a = 'a'; a <= 'z'; a++)
_putchar(a);
}
